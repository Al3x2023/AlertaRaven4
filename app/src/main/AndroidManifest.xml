<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para la app -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- El IDE subraya en amarillo los permisos relacionados con la ubicación y las notificaciones porque, a partir de ciertas versiones de Android, se consideran permisos sensibles o peligrosos. Esto es una advertencia para que te asegures de que estás manejando correctamente las solicitudes de estos permisos en tiempo de ejecución y que realmente los necesitas para la funcionalidad de tu aplicación. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Permisos específicos para dispositivos Vivo -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Características de hardware opcionales para máxima compatibilidad -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.vibrator" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Soporte para diferentes tamaños de pantalla -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
        android:resizeable="true" />

    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Permiso para ACTIVITY_RECOGNITION, condicional para Android 10+ -->
    <uses-permission
        android:name="android.permission.ACTIVITY_RECOGNITION"
        tools:targetApi="29" /> <!-- El IDE puede subrayar `tools:targetApi="29"` si considera que hay una forma más moderna o recomendada de declarar permisos condicionales, o si la versión mínima del SDK de tu proyecto ya es 29 o superior, haciendo esta declaración redundante o innecesaria. También podría ser una advertencia para asegurarse de que el código maneja correctamente los casos en los que el permiso no está disponible en versiones anteriores de Android. -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlertaRaven4"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AlertaRaven4">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <!-- Servicio en primer plano para detección de accidentes -->
        <service
            android:name=".service.AccidentMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Servicio para cancelar alertas -->
        <service
            android:name=".alert.AlertCancelService"
            android:enabled="true"
            android:exported="false" />

        <!-- Receptor para cancelar alertas -->
        <receiver
            android:name=".alert.AlertCancelReceiver"
            android:exported="false" />

    </application>
</manifest>